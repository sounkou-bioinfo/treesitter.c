# Extract enum names from a parsed header

Extract enum names from a parsed header

## Usage

``` r
get_enum_nodes(root)
```

## Arguments

- root:

  A tree-sitter root node.

## Value

Data frame with enum names.
