# tree-sitter language for C

`language()` returns a `tree_sitter_language` object for C for use with
the treesitter package.

## Usage

``` r
language()
```

## Value

A `tree_sitter_language` object.

## Examples

``` r
language()
#> <tree_sitter_language>
#> Language: c
```
